#Lista de paquetes
PACKS="vim wget curl iputils-ping nano procps dnsutils"

#Comandos a ejecutar al crear la imagen separados por ","
COMMANDS="echo ip route del default > /etc/routes,echo ip route add default via 192.168.52.100 >> /etc/routes"

